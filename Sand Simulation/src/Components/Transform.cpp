#include "Transform.h"

/*
void Transfrom::TransformToLocalSpace(glm::vec3 position)
{
	position = glm::inverse(m_ModelMatrix) * glm::vec4(position)
}

void Transfrom::TransformToWorldSpace(glm::vec3 position)
{
	position = m_ModelMatrix * glm::vec4(position)
}
*/